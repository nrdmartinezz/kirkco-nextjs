{
  "workbench.editor.customLabels.patterns": {
    "**/app/**/page.{tsx,ts,jsx,js}": "${dirname(1)}/${dirname} · page",
    "**/app/**/layout.{tsx,ts,jsx,js}": "${dirname(1)}/${dirname} · layout",
    "**/app/**/loading.{tsx,ts,jsx,js}": "${dirname(1)}/${dirname} · loading",
    "**/app/**/error.{tsx,ts,jsx,js}": "${dirname(1)}/${dirname} · error",
    "**/app/**/not-found.{tsx,ts,jsx,js}": "${dirname(1)}/${dirname} · not-found",
    "**/app/**/template.{tsx,ts,jsx,js}": "${dirname(1)}/${dirname} · template",
    "**/app/**/default.{tsx,ts,jsx,js}": "${dirname(1)}/${dirname} · default",
    "**/app/**/route.{ts,js}": "${dirname(1)}/${dirname} · route"
  }
}
